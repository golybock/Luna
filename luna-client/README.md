# Пути приложения:
- base — "/"
- auth — "/login"
- home page — "/[workspaceId]"
- page inside workspace — "/[workspaceId]/[pageId]"
- profile — "/profile" — also modal — "/@modal/profile"
- settings — "/settings" — also modal — "/@modal/settings"